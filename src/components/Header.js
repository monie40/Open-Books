//This Header has no props, so when we invoke it in the App component, the prop should be an empty object
const Header = () => {
    return <div className = 'App-header'>Open Books 📚 </div>
  };

export default Header;